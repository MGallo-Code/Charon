// Package config loads and validates environment variables.
//
// All configuration comes from environment variables (never hardcoded).
// Required: DATABASE_URL, REDIS_URL
// Optional: PORT, SESSION_TTL, COOKIE_DOMAIN, COOKIE_SECURE
package config
